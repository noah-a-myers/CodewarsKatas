def dig_pow(n, p):
    # your code
    strn = str(n)
    total = 0
    i = 0
    it = 1
    while i < len(strn):
        total = total + (int(strn[i])**p)
        p = p +1
        i = i + 1
    if total%n == 0:
        return total/n
    else:
        return -1